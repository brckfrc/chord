# Chord Production Deployment with Existing Reverse Proxy
# Blue-Green Deployment Configuration
#
# Target: Server with existing reverse proxy (Nginx, Traefik, Apache, YunoHost)
# Assumes: You have your own reverse proxy managing SSL and domain routing
#
# Usage:
#   Infrastructure: docker compose -f docker-compose.deploy.yml --profile infra up -d
#   Blue stack:     docker compose -f docker-compose.deploy.yml --profile blue up -d
#   Green stack:    docker compose -f docker-compose.deploy.yml --profile green up -d
#
# For YunoHost: Add override file
#   docker compose -f docker-compose.deploy.yml -f docker-compose.yunohost.yml --profile blue up -d

services:
  # ===========================================
  # INFRASTRUCTURE (Shared)
  # ===========================================
  
  sqlserver:
    image: mcr.microsoft.com/mssql/server:2022-latest@sha256:e07b9699a2b749969f19d86563ceeea22bd3a69f7f1db85a8d1ac4bdaf0c6f56
    container_name: chord-sqlserver
    profiles: ["infra"]
    environment:
      ACCEPT_EULA: "Y"
      SA_PASSWORD: "${SQL_SA_PASSWORD}"
      MSSQL_PID: "Developer"
    ports:
      - "${SQL_PORT:-1433}:1433"
    volumes:
      - sqlserver_data:/var/opt/mssql
      - ./scripts/healthcheck-sql.sh:/healthcheck-sql.sh:ro
    networks:
      chord-network:
        aliases:
          - sqlserver
          - chord-sqlserver
    restart: unless-stopped
    mem_limit: 2g
    healthcheck:
      test: ["CMD-SHELL", "/bin/bash /healthcheck-sql.sh || exit 1"]
      interval: 15s
      timeout: 15s
      retries: 5
      start_period: 60s

  redis:
    image: redis:7-alpine@sha256:6ab0b6e7381779332f97b8ca76193e45b0756f38d4c0dcda72dbb3c32061ab99
    container_name: chord-redis
    profiles: ["infra"]
    ports:
      - "${REDIS_PORT:-6379}:6379"
    volumes:
      - redis_data:/data
    networks:
      chord-network:
        aliases:
          - redis
          - chord-redis
    restart: unless-stopped
    command: redis-server --appendonly yes
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5

  minio:
    image: minio/minio:latest@sha256:14cea493d9a34af32f524e538b8346cf79f3321eff8e708c1e2960462bd8936e
    container_name: chord-minio
    profiles: ["infra"]
    environment:
      MINIO_ROOT_USER: "${MINIO_ROOT_USER:-minioadmin}"
      MINIO_ROOT_PASSWORD: "${MINIO_ROOT_PASSWORD}"
    ports:
      - "${MINIO_API_PORT:-9000}:9000"
      - "${MINIO_CONSOLE_PORT:-9001}:9001"
    volumes:
      - minio_data:/data
    networks:
      - chord-network
    restart: unless-stopped
    command: server /data --console-address ":9001"
    healthcheck:
      test: ["CMD", "mc", "ready", "local"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s

  livekit:
    image: livekit/livekit-server:latest@sha256:2c6869d2d5ff6c9c0166f47be1c92dad6928bfecfa5e4060a6ece48db8accfa3
    container_name: chord-livekit
    profiles: ["infra"]
    environment:
      LIVEKIT_KEYS: "${LIVEKIT_API_KEY:-devkey}: ${LIVEKIT_API_SECRET}"
      LIVEKIT_NODE_IP: "${LIVEKIT_NODE_IP:-}"
    ports:
      - "${LIVEKIT_WS_PORT:-7880}:7880"
      - "${LIVEKIT_RTC_PORT:-7881}:7881/udp"
      - "${LIVEKIT_RTC_PORT:-7881}:7881/tcp"
    volumes:
      - ./backend/livekit.yaml:/livekit.yaml:ro
    command: --config /livekit.yaml --node-ip ${LIVEKIT_NODE_IP:-}
    networks:
      - chord-network
    restart: unless-stopped
    depends_on:
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost:7880"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s

  coturn:
    image: coturn/coturn:latest@sha256:161cedd63c5414c2136f306098e02e9aede74606cedad8b9b8581aae1da4e732
    container_name: chord-coturn
    profiles: ["infra"]
    ports:
      - "${TURN_PORT:-3478}:3478/udp"
      - "${TURN_PORT:-3478}:3478/tcp"
    volumes:
      - ./backend/turnserver.conf:/etc/turnserver.conf:ro
    command: -c /etc/turnserver.conf
    network_mode: "${TURN_NETWORK_MODE:-bridge}"
    restart: unless-stopped

  # ===========================================
  # BLUE STACK
  # ===========================================
  
  api-blue:
    image: ${REGISTRY:-ghcr.io}/${GITHUB_REPO:-chord}/api:${IMAGE_TAG:-latest}
    container_name: chord-api-blue
    profiles: ["blue"]
    depends_on:
      sqlserver:
        condition: service_healthy
    environment:
      - ASPNETCORE_ENVIRONMENT=Production
      - ASPNETCORE_URLS=http://+:80
      - SQL_SA_PASSWORD=${SQL_SA_PASSWORD}
      - SQL_SERVER_HOST=sqlserver
      - SQL_SERVER_PORT=1433
      - DATABASE_NAME=${DATABASE_NAME:-ChordDB}
      - JWT_SECRET=${JWT_SECRET}
      - JWT_ISSUER=${JWT_ISSUER:-ChordAPI}
      - JWT_AUDIENCE=${JWT_AUDIENCE:-ChordClient}
      - JWT_EXPIRY_MINUTES=${JWT_EXPIRY_MINUTES:-60}
      - JWT_REFRESH_TOKEN_EXPIRY_DAYS=${JWT_REFRESH_TOKEN_EXPIRY_DAYS:-7}
      - CORS_ORIGINS=${CORS_ORIGINS}
      - REDIS_CONNECTION=redis:6379
      - MINIO_ENDPOINT=minio:9000
      - MINIO_ACCESS_KEY=${MINIO_ROOT_USER:-minioadmin}
      - MINIO_SECRET_KEY=${MINIO_ROOT_PASSWORD}
      - MINIO_BUCKET_NAME=${MINIO_BUCKET_NAME:-chord-uploads}
      - MINIO_USE_SSL=false
      - LIVEKIT_API_KEY=${LIVEKIT_API_KEY:-devkey}
      - LIVEKIT_API_SECRET=${LIVEKIT_API_SECRET}
      - LIVEKIT_URL=${LIVEKIT_URL:-ws://livekit:7880}
      - LIVEKIT_PUBLIC_URL=${LIVEKIT_PUBLIC_URL:-wss://chord.borak.dev/livekit}
      - RateLimiting__AllowLoadTestBypass=${RateLimiting__AllowLoadTestBypass:-false}
    ports:
      - "5002:80"
    networks:
      - chord-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:80/health"]
      interval: 10s
      timeout: 3s
      retries: 5
      start_period: 30s

  frontend-blue:
    image: ${REGISTRY:-ghcr.io}/${GITHUB_REPO:-chord}/frontend:${IMAGE_TAG:-latest}
    container_name: chord-frontend-blue
    profiles: ["blue"]
    ports:
      - "3002:80"
    networks:
      - chord-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://127.0.0.1:80/health"]
      interval: 10s
      timeout: 3s
      retries: 3
      start_period: 10s

  # ===========================================
  # GREEN STACK
  # ===========================================
  
  api-green:
    image: ${REGISTRY:-ghcr.io}/${GITHUB_REPO:-chord}/api:${IMAGE_TAG:-latest}
    container_name: chord-api-green
    profiles: ["green"]
    depends_on:
      sqlserver:
        condition: service_healthy
    environment:
      - ASPNETCORE_ENVIRONMENT=Production
      - ASPNETCORE_URLS=http://+:80
      - SQL_SA_PASSWORD=${SQL_SA_PASSWORD}
      - SQL_SERVER_HOST=sqlserver
      - SQL_SERVER_PORT=1433
      - DATABASE_NAME=${DATABASE_NAME:-ChordDB}
      - JWT_SECRET=${JWT_SECRET}
      - JWT_ISSUER=${JWT_ISSUER:-ChordAPI}
      - JWT_AUDIENCE=${JWT_AUDIENCE:-ChordClient}
      - JWT_EXPIRY_MINUTES=${JWT_EXPIRY_MINUTES:-60}
      - JWT_REFRESH_TOKEN_EXPIRY_DAYS=${JWT_REFRESH_TOKEN_EXPIRY_DAYS:-7}
      - CORS_ORIGINS=${CORS_ORIGINS}
      - REDIS_CONNECTION=redis:6379
      - MINIO_ENDPOINT=minio:9000
      - MINIO_ACCESS_KEY=${MINIO_ROOT_USER:-minioadmin}
      - MINIO_SECRET_KEY=${MINIO_ROOT_PASSWORD}
      - MINIO_BUCKET_NAME=${MINIO_BUCKET_NAME:-chord-uploads}
      - MINIO_USE_SSL=false
      - LIVEKIT_API_KEY=${LIVEKIT_API_KEY:-devkey}
      - LIVEKIT_API_SECRET=${LIVEKIT_API_SECRET}
      - LIVEKIT_URL=${LIVEKIT_URL:-ws://livekit:7880}
      - LIVEKIT_PUBLIC_URL=${LIVEKIT_PUBLIC_URL:-wss://chord.borak.dev/livekit}
      - RateLimiting__AllowLoadTestBypass=${RateLimiting__AllowLoadTestBypass:-false}
    ports:
      - "5003:80"
    networks:
      - chord-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:80/health"]
      interval: 10s
      timeout: 3s
      retries: 5
      start_period: 30s

  frontend-green:
    image: ${REGISTRY:-ghcr.io}/${GITHUB_REPO:-chord}/frontend:${IMAGE_TAG:-latest}
    container_name: chord-frontend-green
    profiles: ["green"]
    ports:
      - "3003:80"
    networks:
      - chord-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://127.0.0.1:80/health"]
      interval: 10s
      timeout: 3s
      retries: 3
      start_period: 10s

volumes:
  sqlserver_data:
  redis_data:
  minio_data:

networks:
  chord-network:
    driver: bridge


